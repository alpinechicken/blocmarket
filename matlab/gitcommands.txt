Matlab git commit and push to remote:
> source control -> view and commit changes
> source control ->  push (prompts for password))


Powershell commit and push to  remote:
> git add .
> git commit -m "this  is a commit message"
> git push (prompts for github credentials)

Powershell pull from remote:
> git pull

EDIT FROM GITHUB

